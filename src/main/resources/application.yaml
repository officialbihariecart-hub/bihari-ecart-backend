spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/bihariecart
    username: postgres
    password: R@@7091
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080